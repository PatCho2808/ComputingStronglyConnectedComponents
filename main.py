from Graph import *


def compute_number_of_strongly_connected_components(graph):
    # reversed_graph = graph.get_reversed()
    # compute_finishing_times(reversed_graph)
    # DFS_loop(graph)
    return 0
